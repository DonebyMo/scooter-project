class User {
  // User code here
  constructor(userName, password, age) {
    this.userName = userName;
    this.password = password;
    this.age = age;
  }
  login(password) {}

  logout() {}
}

module.exports = User;
